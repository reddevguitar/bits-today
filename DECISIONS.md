# DECISIONS

- Project title: `bit's today`
- The project is now Upbit-centered and altcoin-forward, not BTC-only
- Main cadence is now one autonomous strategy/improvement cycle every 6 hours
- The previous 30-minute lightweight execution model is deprecated
- The agent must maintain 5 preferred buy candidates at all times when data is available
- Preferred buy candidates should focus on Upbit-listed altcoins and exclude the top 4 largest Upbit majors
- Major coins may still be tracked as market context and risk barometers
- The strategy should be aggressive and diversified in paper trading, and should avoid defaulting to passive observation without a strong reason
- Candidate selection should compare turnover, momentum, community attention, news flow, and internal judgment
- Reports and dashboard output should become easier to scan, more visual, and more portal-like over time
- Each 6-hour cycle should include self-evaluation plus at least one concrete improvement to the strategy, report structure, or UI when feasible
- Real exchange execution remains out of scope for now
- External information gathering may use web search, web fetch, and local reasoning, but should stay selective rather than noisy
- In weak breadth sessions with major average range-position under roughly 20 and only 2-3 non-major all-positive 60%+ leaders, prefer a 70% all-positive support basket over defending mega-turnover salvage names.
- In a breadth-snapback session where major average range-position jumps from below roughly 20 to above roughly 80 within one cycle and 2개 이상 held breakout names collapse into clear trap territory, immediately abandon the old basket, raise paper deployment toward roughly 85%, and rebuild around current 85%+ leaders instead of defending the failed morning names.
- In a post-snapback failure cycle where major average range-position collapses by roughly 45 points or more, fresh breakouts fall from 3+ to 0, and 4개 이상 held preferred names lose roughly 45% day-range structure or break invalidation, force an immediate reset toward roughly 70%, keep at most 1 still-healthy quality hold, and rebuild around 5 all-positive survivor/continuation names instead of defending the failed rebound basket.
- In a breadth-rebound session where major average range-position reclaims roughly 60 or higher and multiple non-major leaders already hold 80%+ of the day range with real turnover, reject any same-day gainer that is still about 10% or more below its intraday high; promote cleaner liquid continuation leaders instead of chasing the largest candle.
- When `weak_breadth_warning` clears and top15 non-major low-range count improves by 5 or more within one cycle, do not wait for majors to turn green; immediately purge negative or sub-30% held names and rebuild around all-positive 70%+ leaders plus at most 1 mega-breakout core.
- When majors flip all-green and major average range-position rebounds above roughly 70 while fresh breakouts stay at 0, do not defend yesterday’s high-beta winners if 2개 이상 current holdings break invalidation or fall below roughly 50% of the day range; immediately rebuild around cleaner 60-90% continuation/survivor names and allow at most 1 fading high-beta slot with genuine multi-30B KRW turnover.
- When weak_breadth_warning turns true again while major average range-position still holds roughly 70 or higher, immediately evict any held name that slips to roughly 10% of the day range or lower, trim paper deployment toward roughly 80%, and refill with all-positive continuation/quality names instead of defending the older 85% basket.
- When a same-day mega-turnover leader is still up roughly 25% or more but collapses below roughly 60% of the day range with a day-high gap worse than roughly -15%, classify it as an exhausted blowoff and force the next cycle to rotate into cleaner 75%+ leaders instead of defending the headline volume.
- When weak_breadth_warning clears but 2개 이상 held fresh/beta names already break invalidation in the same cycle, do not average down into the old leaders; immediately reset into 5 all-positive 70%+ leaders where possible and cap the first re-risk step around roughly 85% until fresh-breakout breadth expands again.
- When an 85% all-positive basket is followed within one cycle by a major-average collapse from roughly the 60s to around 30 or lower and 3개 이상 held names slip below roughly 35% of the day range, force an immediate reset toward roughly 70%, keep at most 2 surviving green names, and refill the other slots with current 65%+ all-positive survivors plus at most 1 small clean high-range beta slot.
- When major average range-position rebounds above roughly 60 but weak_breadth_warning stays true and 2개 이상 held same-day leaders are already below roughly 45% of the day range or worse than roughly -15% day-high gap, force the next cycle to cap paper deployment around 80% and rebuild around all-positive 60%+ continuation/quality names plus at most 2 high-beta clean slots instead of defending the older turnover basket.
- When major average range-position falls back below roughly 35 while 3개 이상 current holdings are already below roughly 30% of the day range or break invalidation, force the next cycle to cut paper deployment toward roughly 75%, anchor exactly 1 fresh mega-liquidity leader if available, and rebuild the other 4 slots from cleaner positive survivor names instead of defending the broken prior basket.
- When major average range-position rebounds by roughly 50 points or more, weak_breadth_warning clears, and one or more held salvage cores still sit below roughly 55% of the day range, force a full reset into current 75-100% high-range leaders instead of defending the older salvage basket.
- When an 85% fresh-breakout basket is followed within one cycle by fresh breakouts dropping to 0 and 3개 이상 current holdings breaking invalidation, force an immediate reset toward roughly 80%, abandon the failed breakout names, and rebuild around liquid 65-100% survivor/reclaim leaders even if majors still hold in the 70s.
- When fresh breakouts stay at 0 while a held mega-turnover salvage core flips negative and sits roughly 10% or more below the intraday high, and at least 4 all-positive 60%+ alternatives exist, force that salvage slot out next cycle even if majors remain all-green; rebuild around current positive survivors instead of defending old size.
- When major average range-position rebounds toward roughly 75 or higher while a held high-beta slot slips below roughly 55% of the day range and two or more cleaner 75%+ liquid alternatives already exist, force that slot out next cycle even if price is still above the old invalidation level; upgrade into the cleaner continuation names instead of defending stale beta.
- When major average range-position falls below roughly 25 and only 1 or fewer current holdings remain all-positive while the rest lose their continuation structure, force the next cycle to cut paper deployment toward roughly 70% and rebuild around 5 current all-positive leaders instead of defending the broken basket.
- When a 70% all-positive reset is followed within one cycle by 4개 이상 current holdings breaking invalidation or collapsing under roughly 25% of the day range, but the board still offers 4개 이상 cleaner 80%+ continuation/reclaim names with real turnover, immediately abandon the failed reset and re-upgrade toward roughly 80% around those cleaner names instead of defending the morning survivors out of recency bias.
- When major average range-position falls below roughly 5, fresh breakouts shrink to exactly 1, and 3개 이상 held preferred names break invalidation together, force the next cycle to cut paper deployment toward roughly 70%, keep exactly 1 fresh breakout core, allow up to 2 smaller-liquidity 60-95% positive continuations to complete the 5-name basket, and reject larger negative-turnover traps even if their intraday range-position still looks high.
- When major average range-position rebounds into roughly 40-55 from a sub-20 crash base while weak_breadth_warning stays true and fresh breakouts are only 1, force the next cycle to abandon the old crash basket, rebuild around 5 current all-positive names where possible, and re-risk only toward roughly 75-80% instead of defending the earlier reset winners.
- When major average range-position rebounds toward roughly 80 and many candidates print 95-100% range-position on narrow intraday boxes, allow at most 2 of 5 preferred picks from that tight-box group unless each also has top-5 non-major turnover; keep at least 2 wider-range liquidity leaders so strong breadth is not mistaken for a row of tiny-box pseudo-breakouts.
- When major average range-position cools from roughly the high-70s or higher toward the low-50s within one cycle, weak_breadth_warning flips true, fresh breakouts fall to 0, and 2개 이상 held leaders turn into clear turnover traps, force an immediate reset toward roughly 80%, keep at most 1 small-cap 100% breakout, and rebuild the other 4 slots around all-positive 60-75% continuation/reclaim names instead of defending the failed surge basket.
- When a candidate carries a live security incident, investor-protection notice, or delisting/watchlist headline from major Korean exchanges, do not promote it into preferred buys unless price structure is still clearly superior and the overhang is explicitly acknowledged; default such names to watchlist even if turnover is huge.
- When major average range-position rebounds above roughly 80 while fresh breakouts are still 0, reject any watchlist/security-overhang gainer with worse than roughly -15% day-high gap even if turnover is top-tier, and rotate instead into cleaner 75%+ liquid continuation/reclaim names while allowing paper deployment toward roughly 90%.
- When a 90%-deployed basket is hit by a one-cycle major cooldown (major average drops below roughly 40) but fresh breakouts still remain 3 or more, do not keep broken 30-50% laggards just because breadth is not fully dead; rotate those slots into clean non-watchlist 75%+ leaders and cap deployment around roughly 80% until majors stabilize.
- When majors flip all-green but weak_breadth_warning is true and fresh breakouts stay at 0, do not defend prior-basket names that fall below roughly 50% of the day range or turn negative; immediately rebuild around 5 all-positive continuation names where possible, keep at most 1 90%+ high-beta breakout slot, and cap paper deployment around roughly 80%.
- When major average range-position rebounds above roughly 85, weak_breadth_warning clears, and 2개 이상 held beta names still sit below roughly 50% of the day range or break invalidation, force those broken beta slots out next cycle, raise paper deployment toward roughly 85%, and refill with cleaner 65-85% mega/liquid continuation-reclaim names instead of defending the old breakout basket.
- When major average range-position cools from roughly 85-90 into the low-70s while fresh breakouts recover to 2 or more and breadth stays healthy, keep paper deployment around 85% but force any held 50% range-position laggard out for a 90%+ fresh alternative instead of defending the slower large-cap slot.
- When a candidate has a known same-month token unlock or supply overhang, do not promote it into preferred buys unless it still holds clearly superior upper-range structure; default it to watchlist when a similarly strong cleaner alternative already exists.
- When major average range-position stays above roughly 80 and a previously rejected mega-liquidity attention alt reclaims roughly 75%+ of the day range with a day-high gap better than roughly -2 while one held continuation slot cools below roughly 50%, force the re-entry and rotate out the cooling slot instead of defending the older basket for consistency alone.
- When major average range-position cools from roughly 80+ into the low-60s but fresh breakouts still remain 5 or more, keep paper deployment around roughly 85%, immediately evict any held name that slips below roughly 35% of the day range, and upgrade those slots into current 90%+ leaders instead of defending the prior basket.

- When an 85%-deployed breakout basket cools into weak_breadth_warning=true, fresh breakouts fall to 0, and 4개 이상 current holdings break invalidation or slip below roughly 35% of the day range, force the next cycle to reset around roughly 80%, choose at least 2 liquid 50%+ survivors plus 2 clean 80%+ continuations, and allow at most 1 wide-gap beta slot instead of defending the failed basket.
- When weak_breadth_warning clears, major average range-position rebounds above roughly 55, fresh breakouts stay at 0, and one or more held survivor names slip below roughly 30% of the day range, force those laggards out next cycle, re-risk toward roughly 85%, and rebuild around 80%+ liquid leaders plus 70%+ clean continuations instead of defending the older reset basket.
- When top15 non-major positives expand to 10개 이상 and fresh breakouts recover to 1 or more, but 3개 이상 current holdings cool below roughly 50% of the day range or worse than roughly -5% day-high gap, force an immediate 90% rotation into cleaner 70-100% liquid leaders instead of defending the older rebound basket.
- When a 90%-deployed basket is followed within one cycle by major average range-position collapsing toward roughly 20, top15 non-major positives shrinking to 3 or fewer, and 4개 이상 prior holdings breaking into sub-30% structure or invalidation territory, force an immediate 70% all-positive reset around exactly 1 fresh breakout plus 4 survivor/continuation names, and reject negative mega-turnover salvage candidates unless no cleaner alternatives exist.
- When major average range-position rebounds into roughly 30-45 while weak_breadth_warning stays true and fresh breakouts remain 0, allow at most 1 sub-55 mega-liquidity survivor anchor, and force the other 4 preferred slots into current 60%+ continuation/reclaim names instead of reusing faded earlier breakouts.
- When fresh breakout count is 0, weak_breadth_warning is true, and 4개 이상 held continuation names simultaneously lose roughly 50% day-range structure or break invalidation, force the next cycle to cut deployment toward roughly 75%, keep exactly 1 upper-range leader if available, and fill the other 4 slots with cleaner survivor/reclaim names instead of defending the broken continuation basket.
- When a live Upbit refresh fails or stalls near cycle time, do not churn the paper portfolio on stale prices; keep the last validated 5-name basket, surface the staleness warning prominently, and prioritize refresh reliability in the next improvement cycle.
- When major average range-position rebounds above roughly 85, weak_breadth_warning clears, fresh breakouts stay at 0, and 3개 이상 current holdings are still below roughly 40% of the day range or lose their old leader status, force an immediate 85% reset into current 70%+ liquid continuation/reclaim names instead of defending the stale basket.
- When an 85%-deployed rebound basket is followed within one cycle by major average range-position collapsing from roughly 80+ to below roughly 15 while top15 non-major positives shrink to 3 or fewer, immediately cut paper deployment toward roughly 75%, keep only the real 80%+ survivors, and use the last 2 slots for cleaner 65-75% secondary continuations instead of defending broken mega-turnover names.
- When major average range-position stays below roughly 10 and top15 non-major positives shrink to 2 or fewer, force the next cycle to cut paper deployment toward roughly 60%, require all 5 preferred picks to be non-major all-positive names where possible, and evict flat/negative prior holdings instead of defending them for familiarity alone.
- When major average range-position rebounds above roughly 75 within one cycle while weak_breadth_warning stays true and fresh breakouts remain 0, lift paper deployment only toward roughly 80%, immediately evict held names below roughly 45% of the day range or worse than roughly -7% day-high gap, and rebuild around liquid 70-85% continuation names plus at most 1 smaller high-range beta slot.
- When weak_breadth_warning clears, major average range-position sits roughly 55-70, fresh breakouts are only 1, and 2개 이상 held names break invalidation or collapse below roughly 20% of the day range, raise paper deployment only toward roughly 85%, keep 2-3 liquid continuation anchors, and replace the broken slots with current 80%+ high-range leaders instead of waiting for a full breakout expansion.
- When weak_breadth_warning is true, major average range-position sits roughly in the 40-55 zone, fresh breakouts are 0, and top15 non-major positives shrink to 1 or fewer, force the next cycle to cut paper deployment toward roughly 75%, rebuild around 5 all-positive non-major names where possible, and allow at most 2 sub-5B KRW turnover slots only if each still holds roughly 70%+ of the day range; do not keep yesterday's negative 10B+ names for familiarity alone.
- When major average range-position falls below roughly 40 while `weak_breadth_warning` is true, fresh breakouts stay at 0, and top15 non-major low-range count reaches 8 or more, keep deployment around roughly 75%, allow at most 1 wide-gap mega-attention slot, and force out prior core holdings that slip below roughly 35-50% structure even if they remain slightly positive.
